<link rel="stylesheet" href="comments.scss"/>
<center>
<%= form_for([@product, @product.comments.build]) do |f|%>

<%= f.text_area :body%><br>
<%= hidden_field_tag 'comment[user_id]', nil%>

<%= f.submit "Залишити коментар", :class => 'but'%>  
</center>

<%end%>