<%= form_for([@product, @product.comments.build]) do |f|%>
<p>
<%= f.label :body%><br>
<%= f.text_area :body%><br>

</p>

<%= hidden_field_tag 'comment[user_id]', nil, :value => "#{current_user.id}"%>

<p>
<%= f.submit %>
</p>

<%end%>